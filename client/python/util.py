# -*- coding: iso-8859-1 -*-
from library import library


##Given the x,y and size of two bots, returns the Manhattan distance
def distance(fromX, fromY, fromSize, toX, toY, toSize):
  return library.distance(fromX, fromY, fromSize, toX, toY, toSize)
  
  
